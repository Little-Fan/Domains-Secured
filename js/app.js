/*global define */define([	'marionette',	'views/Header',	'views/LoginItemView',	'views/Footer'], function (Marionette, Header, LoginItemView, Footer) {	'use strict';	var app = new Marionette.Application();	var header = new Header();	var main = new LoginItemView();	var footer = new Footer();	app.addRegions({		header: '#header',		main: '#main',		footer: '#footer'	});	app.addInitializer(function () {		app.header.show(header);		app.main.show(main);		app.footer.show(footer);	});	return window.app = app;});